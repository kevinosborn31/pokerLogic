# Poker Logic

Simple project to determine the value of a poker hand with a node input

In the file directory, use node poker.js followed by your "hand" in quotations in the following format: 2D is 2 of Diamonds, KH is King of Hearts etc.

Example input: "node poker.js "2H JS QS KS AS"
